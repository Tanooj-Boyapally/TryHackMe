this room helps us to crack hashes, and teaches how to crack hash passwords, ssh keys "idrsa", cracking of ZIP files, RAR files, windows authentication hashes,
cracking of /etc/shadow file hashes, find the hash type like hash_identifier
using of --format=raw-md5, --format=raw-sha512....

key points are

ssh2john
rar2john
zip2john
john --wordlist=<wordlist.txt> [hash_file]
john zip.zip > zip_hash.txt
